# Chezmoi dotfiles manager PATH setup
# This script adds ~/.local/bin to the PATH environment variable to support
# user-installed tools like chezmoi and other local binaries.
export PATH="$HOME/.local/bin:$PATH"
