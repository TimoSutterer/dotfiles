# Zsh completions plugin
# This plugin provides additional completion definitions for many common
# commands that aren't included by default in Zsh's built-in completion system.
#
# Features:
# - Extensive collection of completion definitions for popular tools
# - Enhanced tab completion for commands like git, docker, npm, etc.
# - Automatically integrates with Zsh's completion system
# - Regular updates with new completion definitions

# Install the zsh-completions plugin using zinit
zinit light zsh-users/zsh-completions